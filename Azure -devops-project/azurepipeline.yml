trigger:
- main

pool:
  name: Default

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '18.x'

- script: |
    cd app
    npm install
  displayName: 'Install dependencies'

- script: |
    echo "Build successful"
  displayName: 'Build Step'